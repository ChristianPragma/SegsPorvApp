import * as React from 'react';
import { Text, View } from 'react-native';

function ContainerTrackMe() : JSX.Element  {
    return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text>Rastréame!</Text>
      </View>
    );
  }

export default ContainerTrackMe